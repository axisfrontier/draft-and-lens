# Draft & Lens — UI Design Exploration (Backlog)

**Status: not started. Sequenced after all build work + codebase audit.**

## Origin
Raised during Noel's beta feedback review. Noel's specific complaints:
- Contrast issues (light grey on black nav, small copy) — **already fixed**, separate from this item
- "Generic AI" visual impression — specifically called out umber/amber tones as reading as generic-AI aesthetic

## The task
Explore one representative feedback/report screen as a fresh visual design direction — via the Visualizer, in-chat — **before** committing to any full redesign. This is deliberately scoped as a single exploratory screen, not a redesign brief.

Goal: something that reads as considered and editorial, not "generic AI product" — directly answering Noel's aesthetic critique, not just the contrast/legibility issues (which are separate and already resolved).

## Sequencing — confirmed
1. All current build work (detection, timeline reasoning, mentor mode, differentiator messaging)
2. Planned periodic codebase audit (per AUDIT_CHECKLIST.md, Level 3 — triggered before major new feature work)
3. **Then** this — new UI exploration begins

## Not yet decided
- Scope of "full redesign" if the exploration screen lands well (whole app? report view only? homescreen too?)
- Whether this uses the existing 21-mockup-style batch process (that was Codex-Maths' method, not necessarily right for D&L) or a different approach

## Standing rule going forward
Any product-shaping discussion that isn't immediately actioned must be written to a file in this repo before the session ends — not left to live only in chat history. This file is the fix for that gap.
