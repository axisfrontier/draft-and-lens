import { NextResponse } from 'next/server';

/** POST /api/webhooks/stripe — Stage 7 */
export async function POST() {
  return NextResponse.json({ error: 'Not implemented — Stage 7' }, { status: 501 });
}
