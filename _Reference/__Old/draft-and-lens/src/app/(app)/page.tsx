/**
 * Stage 0 scaffold — upload screen placeholder.
 * Full upload flow (must-choose type selector) arrives in Stage 4.
 */
export default function AppHomePage() {
  return (
    <main className="min-h-screen bg-paper p-8">
      <h1 className="font-serif text-2xl text-ink">Draft &amp; Lens</h1>
      <p className="mt-4 text-ink-soft">Stage 0 scaffold — upload UI in Stage 4.</p>
    </main>
  );
}
