# Marketing route group

§05 lists `(marketing)/page.tsx` for a public landing. It is **not mounted at /** in Stage 0
because `(app)/page.tsx` (upload screen) occupies `/` per the prototype.

Stage 4 will add middleware or path strategy to resolve this. Pricing is at `/pricing`.
