# Dependencies
node_modules/

# Next.js build output
.next/
out/
build/
dist/

# Environment files & secrets — NEVER commit real keys
.env
.env.local
.env.local*
.env*.local
**/.env.local*
**/API KEY.txt
**/*api*key*
**/*API*KEY*
*.key

# Editor / OS junk
.DS_Store
.aider*

# Logs
*.log
npm-debug.log*

# Test / extraction caches
scripts/.extracted/
.vercel
.env*

# Legal / trademark / launch-prep material — stays on disk, never enters git
# history (added 1 Aug 2026, ahead of the migration out of Dropbox).
/DraftAndLens_Solicitor_Questions.md
/DraftAndLens_BetaOutreach_Tracker.xlsx
/DraftAndLens_GoLivePlan.md
/DraftAndLens_Anthropic_Terms_Record.md
/DraftAndLens_Trademark_Search_Record.md
/URGENT TIMETABLE/
/for Solicitor/

# Generated knowledge graph — rebuilt by `graphify update .`, never authored.
# 4MB of derived output that would churn on every code change.
/graphify-out/

# One-time audit reference, deleted 20 Aug 2026. Nothing unique was in it —
# the same palette lives in draft-and-lens/src/design/tokens.css, in
# DraftAndLens_DesignSystem.md, and in the prototype. Ignored so a re-export
# cannot land back in the repo.
/DraftAndLens_Prototype_Component_CSS.css

# Campaign exploration — image work, not codebase. Deleted 20 Aug 2026.
/Ads/


# Large working assets — kept locally, not in the repo (2026-08-22).
# Reference material and marketing artwork rather than anything the build
# reads: Inspiration/ is 1.2M and "Ad concepts/" 380K, and neither is imported
# by a single line of code. The lens portraits the app actually serves live in
# draft-and-lens/public/lenses/ and stay tracked.
Ad concepts/
Inspiration/
draftandlens.png
